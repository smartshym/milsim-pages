# Исходники и спойлеры — НЕ публикуются на GitHub Pages.
# В _src лежат генератор (build.js, содержит все коды и раскладку ложных целей),
# бриф и ключи гейм-мастера. Эти файлы остаются только локально.
_src/
CONTEXT.md

# Бэкап зашифрованной версии админки (для отката) — не публикуем.
admin/index.locked.html.bak

# Прочее
.DS_Store
Thumbs.db
node_modules/
