# Работаем только над движком. Всё, кроме engine/, — локально (в remote/Pages не уходит).
/*
!/.gitignore
!/engine/

# Дампы БД и сиды — локальные (сид содержит коды-спойлеры). Скрипты в engine/db/ остаются.
engine/db/dumps/
engine/db/seeds/
